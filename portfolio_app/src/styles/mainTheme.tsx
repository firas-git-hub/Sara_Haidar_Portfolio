import { SxProps, Theme } from "@mui/material";

export const getLocaleSwitcherButtonStyle = (): SxProps<Theme> => {
    return ({
        padding: '0.3rem',
        minWidth: '0',
        borderWidth: '0.125rem',
        lineHeight: 'normal',
        fontWeight: 'bold'
    })
}

export const getNavbarButtonStule = (): SxProps<Theme> => {
    return ({
        padding: '0.3rem',
        minWidth: '0',
        borderWidth: '0.125rem',
        lineHeight: 'normal',
        fontWeight: 'bold'
    })
}